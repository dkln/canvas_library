HTML Canvas Javascript Library (in the works)

Especially handy when you're used to the structure of Flash / Actionscript 3.0.

Usage:

  <canvas id="the_screen"></canvas>

  dc = new canvaslib.DisplayContainer("the_screen");
  
  other_obj = new canvaslib.DisplayContainer();
  dc.addChild(other_obj);